# Core: required for config load, arc load, pipeline, CI, and systems test.
pyyaml>=5.0

# Pearl News: feed ingest + WordPress publish
feedparser>=6.0
requests>=2.28

# CI: ops artifact schema validation (scripts/ci/validate_ops_artifacts.py)
jsonschema>=4.0

# Tests
pytest>=7.0
